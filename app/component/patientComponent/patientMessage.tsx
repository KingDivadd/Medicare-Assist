import { PatientTabProps } from '@/types'
import React from 'react'

const PatientMessage = ({patient_tab, setPatient_tab}:PatientTabProps) => {
    return (
        <div>PatientMessage</div>
    )
}

export default PatientMessage