import { PatientTabProps } from '@/types'
import React from 'react'

const PatientDocument = ({patient_tab, setPatient_tab}:PatientTabProps) => {
    return (
        <div>PatientDocument</div>
    )
}

export default PatientDocument